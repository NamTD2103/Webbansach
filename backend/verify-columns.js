const oracledb = require('oracledb'); require('dotenv').config(); (async () => { let c; try { c = await oracledb.getConnection({user:'system',password:'123456',connectString:'localhost:1521/orcl21pdb1'}); const r = await c.execute('SELECT COLUMN_NAME, DATA_TYPE FROM USER_TAB_COLUMNS WHERE TABLE_NAME='USERS' ORDER BY COLUMN_ID'); r.rows.forEach(row => console.log(row[0] + ': ' + row[1])); await c.close(); } catch(e) { console.error(e.message); } })();